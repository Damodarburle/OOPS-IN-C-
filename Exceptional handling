#include <iostream>
using namespace std;
int main(){
  int a,b;
  cin>>a>>b;
  double sum=10;
  try {
    sum=sum/(a-b);
    if((a-b)!=0){
      cout<<sum;
    }
    else {
      throw sum;
    }
  }
  catch (double sum){
    cout<<"denominator is 0!!!!!"<<endl;
  }
  return 0;
}
